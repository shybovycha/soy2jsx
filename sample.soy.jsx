ServiceDesk.Templates.Agent.Reports.reportMainView = (
    {
        projectKey,
        projectName,
        selectedReport,
        selectedTimescale,
        canEditReports,
        isEditing,
        showHelpBubble
    }
) => <div id={`sd-report-content ${var1}${moo()} `} />;