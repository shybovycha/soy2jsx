{
  "name": "soy2jsx",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "compile-grammar-dev": "./node_modules/.bin/pegjs -o SOY.js --trace --allowed-start-rules SOY SOY.pegjs ",
    "compile-grammar": "./node_modules/.bin/pegjs -o SOY.js --allowed-start-rules SOY SOY.pegjs ",
    "test": "npm run-script compile-grammar && node index.js sample.soy"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "pegjs": "^0.10.0",
    "glob": "^7.1.2",
    "recast": "^0.12.9"
  }
}
